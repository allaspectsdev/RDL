version: "3.8"

services:
  datalens:
    build: .
    container_name: datalens-app
    restart: unless-stopped
    ports:
      - "8501:8501"
    volumes:
      - upload_data:/app/uploads
    environment:
      - STREAMLIT_SERVER_MAX_UPLOAD_SIZE=200
    networks:
      - datalens-net
    deploy:
      resources:
        limits:
          memory: 2G
          cpus: "2.0"

  nginx:
    image: nginx:alpine
    container_name: datalens-nginx
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/datalens.conf:/etc/nginx/conf.d/default.conf:ro
      - certbot_webroot:/var/www/certbot:ro
      - certbot_certs:/etc/letsencrypt:ro
    depends_on:
      - datalens
    networks:
      - datalens-net

  # Optional: auto-renew SSL certs
  certbot:
    image: certbot/certbot
    container_name: datalens-certbot
    volumes:
      - certbot_webroot:/var/www/certbot
      - certbot_certs:/etc/letsencrypt
    entrypoint: "/bin/sh -c 'trap exit TERM; while :; do certbot renew; sleep 12h & wait $${!}; done;'"

volumes:
  upload_data:
  certbot_webroot:
  certbot_certs:

networks:
  datalens-net:
    driver: bridge
